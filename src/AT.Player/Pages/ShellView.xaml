<Window x:Class="AT.Player.Pages.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:s="https://github.com/canton7/Stylet"
        xmlns:local="clr-namespace:AT.Player.Pages"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        d:DataContext="{d:DesignInstance local:ShellViewModel}"
        Title="AT.Player" Height="{Binding Height, Mode=TwoWay}" Width="{Binding Width, Mode=TwoWay}" Top="{Binding Top, Mode=TwoWay}" Left="{Binding Left, Mode=TwoWay}">

    <DockPanel>

        <ContentControl DockPanel.Dock="Top" s:View.Model="{Binding Taskbar}" />

        <materialDesign:Snackbar DockPanel.Dock="Bottom" MessageQueue="{Binding SnackbarMessageQueue}" />

        <TabControl Style="{StaticResource StyletConductorTabControl}" />
    </DockPanel>
</Window>